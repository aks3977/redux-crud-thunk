{
  "name": "json_server",
  "version": "1.0.0",
  "description": "mock api server",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch db.json --port 4000"
  },
  "author": "abhishek",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
